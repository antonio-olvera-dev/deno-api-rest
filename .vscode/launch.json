{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Deno",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "runtimeExecutable": "deno",
            "runtimeArgs": ["run", "-A", "main.ts"], //---Cambiar main.ts por la ruta de tu archivo principal ej: index.ts---
            "outputCapture": "std",
            "port": 8000
        }
    ]
}

//"${fileBasename}" Nombre del archivo de la pestaña actual
